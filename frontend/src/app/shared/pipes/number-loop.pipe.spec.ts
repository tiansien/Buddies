import { NumberLoopPipe } from './number-loop.pipe';

describe('NumberLoopPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberLoopPipe();
    expect(pipe).toBeTruthy();
  });
});
